name: mqtt-operator
description: Mqtt Operator is to manage Mqtt service instances deployed on the Kubernetes cluster. It is built using the Operator SDK, which is part of the Operator Framework.
type: public
branches:
  - name: master
    type: protected