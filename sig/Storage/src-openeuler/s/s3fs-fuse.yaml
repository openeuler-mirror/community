name: s3fs-fuse
description: s3fs allows Linux and macOS to mount an S3 bucket via FUSE. s3fs preserves
  the native object format for files, allowing use of other tools like AWS CLI
upstream: https://github.com/s3fs-fuse/s3fs-fuse
branches:
- name: master
  type: protected
type: public
