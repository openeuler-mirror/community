name: perl-Context-Preserve
upstream: http://search.cpan.org/dist/Context-Preserve/
description: Run code after a subroutine call, preserving the context the subroutine
  would have seen if it were the last statement in the caller
branches:
- name: master
  type: protected
- name: openEuler-20.09
  type: readonly
  create_from: master
type: public
