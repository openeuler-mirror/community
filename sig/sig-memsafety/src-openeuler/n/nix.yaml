name: nix
description: Nix seeks to provide friendly bindings to various *nix platform APIs (Linux, Darwin, ...). The goal is to not provide a 100% unified interface, but to unify what can be while still providing platform specific APIs.
upstream: https://github.com/nix-rust/nix
branches:
- name: master
  type: protected
type: public
