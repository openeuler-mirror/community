name: fast_rsync
description: A faster implementation of librsync in pure Rust, using SIMD operations where available. Note that only the legacy MD4 format is supported, not BLAKE2.
upstream: https://github.com/dropbox/fast_rsync
branches:
- name: master
  type: protected
type: public
