name: rubygem-coffee-rails
upstream: https://github.com/rails/coffee-rails
description: CoffeeScript adapter for the Rails asset pipeline. Also adds support
  to use CoffeeScript to respond to JavaScript requests (use .coffee views).
branches:
- name: master
  type: protected
type: public
