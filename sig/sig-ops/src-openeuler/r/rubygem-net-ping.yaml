name: rubygem-net-ping
upstream: https://github.com/eitoball/net-ping
description: The net-ping library provides a ping interface for Ruby. It includes
  separate TCP, HTTP, LDAP, ICMP, UDP, WMI (for Windows) and external ping classes.
branches:
- name: master
  type: protected
type: public
