name: puppet-agent-puppet-strings
upstream: https://github.com/puppetlabs/puppet-strings
description: Puppet Strings generates documentation for Puppet code and extensions
  written in Puppet and Ruby. Strings processes code and YARD-style code comments
  to create documentation in HTML, Markdown, or JSON formats.
branches:
- name: master
  type: protected
type: public
