name: python-setuptools_scm_git_archive
description: This is a setuptools_scm plugin that adds support for git archives (for example the ones GitHub automatically generates).
upstream: https://github.com/Changaco/setuptools_scm_git_archive
branches:
- name: master
  type: protected
type: public
