name: python-frontmatter
upstream: https://github.com/eyeseast/python-frontmatter
description: A small package to load and parse files (or just text) with YAML (or JSON, TOML or other) front matter.
branches:
- name: master
  type: protected
type: public
