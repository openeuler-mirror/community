name: pin-llvm-client
description: A Pin (Plug-IN framework) client is implemented based on Clang plugin and can execute the compiler optimization pass in LLVM.
branches:
- name: master
  type: protected
type: public
