name: R-prettycode
description: Replace the standard print method for functions with one that performs syntax highlighting, using ANSI colors, if the terminal supports them.
upstream: https://github.com/r-lib/prettycode
branches:
- name: master
  type: protected
type: public
